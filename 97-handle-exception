public String divide(Integer a, Integer b){
    
    Integer division = 0; 
    try{
        if(b == 0) return 'Divide by 0';

        division = a/b; 
        return Integer.valueOf(division);

    }catch(Expection e){
        return e.getMessage()
    }
}