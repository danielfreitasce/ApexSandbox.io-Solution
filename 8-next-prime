public Integer nextPrime(Integer num) {
            //5
    Integer nextPrimeNum = num+1; 
    Boolean isNotPrime = false; 

    for(Integer i = 2; i <nextPrimeNum; i++){
        if(Math.mod(nextPrimeNum, i) == 0){
            isNotPrime = true;
            break;
        }
    }

    if(isNotPrime){
        nextPrime(nextPrimeNum);
    }
    else{
        return nextPrimeNum; 
    }
    return 0; 
}
