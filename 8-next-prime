public Integer nextPrime(Integer num) {

    if(num < 1) num = 1; //Se for menor que zero, iguala a 1 para encontrar o número primo mais próximo.
    
    Integer nextPrimeNum = num+1; 
    Boolean isNotPrime = false; 

    for(Integer i = 2; i <nextPrimeNum; i++){
        if(Math.mod(nextPrimeNum, i) == 0){
            isNotPrime = true;
            break;
        }
    }

    if(isNotPrime){
        nextPrimeNum = nextPrime(nextPrimeNum);
    }

    return nextPrimeNum; 
}
