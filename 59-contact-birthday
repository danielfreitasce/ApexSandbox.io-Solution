public Boolean isBirthday(Contact c) {
   
   Date todayDate = System.today(); 
   
   if(c.Birthdate?.day() == todayDate.day() && c.Birthdate?.month() == todayDate.month())
        return true; 
    
   return false;
}