public Boolean sameParent(Account acc, Contact con, Opportunity opp) {

     if(con.AccountId != null && opp.AccountId !=null && acc != null){
        if(con.AccountId == acc.Id && opp.AccountId == acc.Id) return true; 
    }
    return false;  
}