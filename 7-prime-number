public Boolean isPrime(Integer num) {
    
    Boolean isPrimeNum = true; 

    for(Integer i = 2; i < num; i++){
        if(Math.mod(num, i) == 0){
            isPrimeNum = false;
            break; 
        } 
    }
    return isPrimeNum;
}